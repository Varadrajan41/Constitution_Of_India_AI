chromadb>=0.5.0
ollama>=0.3.0
streamlit>=1.36.0
pypdf>=4.2.0
# Embeddings (bge-base-en-v1.5) + reranker (bge-reranker-v2-m3).
# Pulls torch + transformers; install a CUDA torch build for GPU.
sentence-transformers>=3.0.0
# For scripts/setup_saul.sh (download the SaulLM GGUF):
huggingface_hub>=0.23.0
