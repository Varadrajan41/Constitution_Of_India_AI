# Vector store (regenerated by ingest)
chroma_db/

# Chunk previews (regenerated by backend.dump)
preview/

# Saved eval run transcripts
eval/runs/

# Source corpus + cached extraction
# The official Constitution PDF IS committed so the repo runs out-of-the-box.
# The extracted .txt is a rebuildable cache (regenerated by backend.extract).
data/*.pdf
data/*.txt
!data/.gitkeep
!data/Constitution of India.pdf

# Python
__pycache__/
*.pyc
.venv/
venv*/
.env
